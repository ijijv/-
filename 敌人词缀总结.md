# 《志怪塔》敌人词缀总结

> 用途：后期敌人（第二幕起）随机携带「词缀」强化，不只加数值、加机制。这份总结供你**据此设计 Boss 机制**——Boss 不用随机词缀，而是把这些词缀做成**专属、固定的机制**（更强、更有辨识度）。
> 当前词缀已在 `原型.html` 落地（`AFFIXES` 数组 + `rollAffixes` 随机发放 + 敌人面板金色词缀标签）。

---

## 一、词缀出现规则

| 幕 | 普通敌人 | 精英敌人 | Boss |
|----|----------|----------|------|
| 第一幕 | 无词缀 | 1 个词缀 | 无词缀（用专属机制） |
| 第二幕 | 1 个词缀 | 2 个词缀 | 无词缀（用专属机制） |
| 第三幕 | 2 个词缀 | 3 个词缀 | 无词缀（用专属机制） |

- 词缀随机、不重复（洗牌后取前 N 个）。
- 难度靠「更多不同词缀叠加」而非「数值翻倍」：所有词缀统一基础值（tier=1），第三幕通过叠加更多词缀提升强度，组合随机性更高。

---

## 二、九大词缀一览

| 词缀 | 触发时机 | 数值 | 说明 |
|------|----------|------|------|
| **勾魂** | 攻击时 | 勾魂 1 层 | 你下回合少抽 1 张牌 |
| **反震** | 你打它时 | 反弹 3 点伤害 | 受击反伤（真实扣血，不吃格挡） |
| **再生** | 每回合开始 | 回复 4 点生命 | 越拖越难打死 |
| **刚体** | 每回合开始 | 力量 +1 | 攻击越叠越痛 |
| **屏壁** | 进场时 | 自带 8 点格挡 | 开局一层盾，永久保留直到被击碎 |
| **剧毒** | 攻击时 | 额外 +2 伤害 | 单次攻击更痛 |
| **蚀血** | 攻击时 | 回复 3 点生命 | 打你就回血（吸血） |
| **怨气缠身** | 攻击时 | 塞 1 张【怨气】废牌 | 往你手牌塞废牌（手牌最多 3 张） |
| **灵化** | 被动 | 免疫 | 免疫负面效果（镇魂、诅咒、魅惑等） |

> 数值来源：`AFFIXES` 数组的 `desc` 只写文字说明，实际数值硬编码在对应代码 hook 里（勾魂/剧毒/蚀血在 `dealDamageToPlayer`，反震在 `dealDamageTo`，再生/刚体在 `enemyAct`，屏壁在 `makeEnemy`，怨气缠身塞废牌在 `addJunkToHand`，灵化免疫在 `applyCurse`/镇魂/魅惑处用 `isLinghua` 拦截）。

---

## 三、词缀触发链路（技术备忘）

| 词缀 | 挂载点（函数） | 关键代码位置 |
|------|----------------|--------------|
| 屏壁 | `makeEnemy`（进场） | `e.block = e.pingbi = 8`（永久保留，`enemyAct` 开回合重置为 `pingbi`，直到被击碎） |
| 再生 / 刚体 | `enemyAct`（每回合，`e.block` 重置之后） | 回血 / `strength += tier` |
| 反震 | `dealDamageTo`（你打它结算后） | `G.hp -= 3*tier` |
| 剧毒 | `dealDamageToPlayer`（敌人攻击前） | `d += 2*tier` |
| 蚀血 / 勾魂 | `dealDamageToPlayer`（敌人攻击后） | 回血 / `G.gouhun=1` |
| 怨气缠身 | `dealDamageToPlayer`（敌人攻击后） | `addJunkToHand(1)`（废牌上限 3） |
| 灵化 | `applyCurse` / 镇魂 / 魅惑（施加负面时） | `isLinghua(e)` 拦截并跳过施加 |

---

## 四、用词缀设计 Boss 机制（建议）

Boss 不随机带词缀，而是把词缀「固定 + 放大」成专属技能，例如：

- **旱魃**（第一幕）→ 把「威压 + 剧毒」做成固定机制：每回合灼烧你、攻击更痛。
- **血尸**（第一幕）→ 把「蚀血」放大成核心：攻击回血比例更高。
- **判官王**（第二幕）→ 把「反震」做成「生死簿清算」：记账你打出的伤害、延迟反弹。
- **修罗王**（第三幕）→ 把「反震 + 剧毒」做成「业火」：你出攻击牌就反伤。
- **阎罗天子**（第三幕）→ 把「再生 + 刚体 + 屏壁」组合成「轮回两阶段」：二阶段回血 + 增长 + 上盾。

**设计小抄**：Boss 机制 = 选 1~2 个词缀主题 + 一个 Boss 专属的「招牌动作」（召唤 / 阶段切换 / 记账 / 失忆），比普通敌人更强的数值即可。

---

## 五、当前敌人意图类型（可复用做 Boss 招数）

`ENEMIES` 里已支持的意图类型（`intents` 数组元素）：

- `attack`（v = 伤害）
- `multi`（n = 次数，v = 每段伤害）
- `strength`（v = 力量 +N）
- `block`（v = 格挡）
- `heal`（v = 回血）
- `charge`（v = 蓄力，下回合伤害 +N）
- `attackBlock`（v = 攻击，block = 格挡）
- `weak`（对你施加虚弱）
- `gouhun`（对你施加勾魂）
- `discard`（你下回合弃 1 张）
- `summon`（n = 召唤小僵尸数量）
- `aoe`（群伤 + 驱散召唤，现已从夜王移除，但意图类型仍保留可复用）
- `dormant`（首回合休眠观望）

> 设计新 Boss 时，`intents` 数组可以直接用上面这些类型组合；需要全新机制（如「燥热」「生死簿」「业火」「失忆封印」）时告诉我，我来加对应的新意图类型。
